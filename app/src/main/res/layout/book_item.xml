<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/book_image"
        style="@style/BookItemImage"
        android:layout_width="@dimen/book_image_width"
        android:layout_height="@dimen/book_image_height"
        android:contentDescription="@string/app_name"
        tools:src="@drawable/book_placeholder" />

    <TextView
        android:id="@+id/book_name"
        style="@style/BaseTextStyle.Medium.Red"
        android:layout_width="@dimen/book_item_width"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/book_name_margin"
        android:gravity="center_horizontal"
        tools:text="@string/test_book_name" />

</LinearLayout>